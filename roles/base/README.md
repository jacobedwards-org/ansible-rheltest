Ansible base role
=================

This role installs base packages. In the future it may change the
default behavior of login shells, set the motd, etc.

Variables
---------

- `base_packages`

   The `base_packages` variable declares which packages should be
   installed in the base system.
